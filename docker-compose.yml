version: '3.9'

services:
  app:
    build: .
    container_name: boycott-app
    ports:
      - "5000:5000"
    volumes:
      - .:/app
      - ./database.db:/app/database.db
    environment:
      - FLASK_ENV=development
